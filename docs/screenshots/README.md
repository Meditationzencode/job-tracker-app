# Screenshots

Drop the following PNG files here for the main README to render correctly:

- `dashboard.png` — full dashboard with status counts, charts, upcoming interviews and deadlines (recommended: ~1400×900)
- `jobs-list.png` — Jobs list with the search field, status filter, and sort dropdown visible
- `job-detail.png` — a job's detail page showing status, an interview, and a contact
- `mobile.png` — same dashboard at 375 px wide (iPhone SE) with the hamburger menu open

Suggested workflow:

1. Run `python manage.py seed_demo` to populate the demo account.
2. Sign in with `demo@example.com` / `demopass123`.
3. Use Chrome / Edge devtools (Ctrl+Shift+M) to switch to mobile view for `mobile.png`.
4. Save each screenshot here with the exact filename above.

Keep file sizes under ~300 kB by exporting as compressed PNG (or use a tool like [Squoosh](https://squoosh.app/)).
